import React from 'react';

import Header from '../Header/Header';
import s from './CV.module.css';

const CV = () => {
  return (
    <div className={s.cv}>
      <Header />
    </div>
  );
};

export default CV;
